services:
  frontend:
    depends_on:
      - backend

  backend:
    depends_on:
      - db

  db:
    image: mongo:6
    container_name: mongo_db

    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: